<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Inter", sans-serif;
      }

      body {
        background-color: #f8fafc;
        color: #1e293b;
        min-height: 100vh;
      }

      .layout {
        display: flex;
        min-height: 100vh;
      }

      .sidebar {
        width: 260px;
        background: #0f172a;
        color: #e2e8f0;
        padding: 1rem;
        position: sticky;
        top: 0;
        height: 100vh;
      }

      .sidebar .brand {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        padding: 0.35rem 0.25rem 0.9rem;
        border-bottom: 1px solid rgba(148, 163, 184, 0.18);
        margin-bottom: 0.9rem;
      }

      .sidebar .brand .logo {
        width: 34px;
        height: 34px;
        border-radius: 10px;
        background: linear-gradient(135deg, rgba(59,130,246,0.9), rgba(139,92,246,0.9));
        box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
      }

      .sidebar .brand .title {
        display: flex;
        flex-direction: column;
        line-height: 1.1;
      }

      .sidebar .brand .title .name {
        font-size: 0.95rem;
        font-weight: 700;
        color: #f1f5f9;
        letter-spacing: 0.2px;
      }

      .sidebar .brand .title .sub {
        font-size: 0.75rem;
        color: #94a3b8;
        margin-top: 2px;
      }

      .nav {
        display: flex;
        flex-direction: column;
        gap: 0.9rem;
      }

      .nav .group {
        display: flex;
        flex-direction: column;
        gap: 0.35rem;
      }

      .nav .group-title {
        font-size: 0.72rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: #94a3b8;
        padding: 0.2rem 0.35rem;
      }

      .nav .group-items {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
      }

      .nav a {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        color: #cbd5e1;
        padding: 0.55rem 0.6rem;
        border-radius: 0.5rem;
        transition: background 0.15s, color 0.15s;
      }

      .nav a:hover {
        background: rgba(148, 163, 184, 0.12);
        color: #f1f5f9;
      }

      .nav a.active {
        background: rgba(59, 130, 246, 0.22);
        color: #f8fafc;
        box-shadow: inset 0 0 0 1px rgba(59, 130, 246, 0.35);
      }

      .nav .hint {
        font-size: 0.75rem;
        color: #94a3b8;
        margin: 0.15rem 0 0.4rem 0.6rem;
        line-height: 1.2;
      }

      .content {
        flex: 1;
        padding: 1rem;
      }

      .topbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
      }

      .topbar h1 {
        font-size: 1.25rem;
        color: #0f172a;
      }

      .pill {
        display: inline-block;
        padding: 0.1rem 0.4rem;
        border-radius: 999px;
        font-size: 0.75rem;
        background: #f1f5f9;
        color: #334155;
      }

      .mono {
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
          "Liberation Mono", "Courier New", monospace;
        font-size: 0.8rem;
      }

      @media (max-width: 980px) {
        .sidebar {
          width: 220px;
        }
      }

      @media (max-width: 768px) {
        .layout {
          flex-direction: column;
        }
        .sidebar {
          width: 100%;
          height: auto;
          position: static;
        }
      }
    </style>
  </head>
  <body>
    <div class="layout">
      <aside class="sidebar">
        <div class="brand">
          <div class="logo"></div>
          <div class="title">
            <div class="name">Dashboards</div>
            <div class="sub">Superhero API</div>
          </div>
        </div>
        <div class="nav">
          <div class="group">
            <div class="group-title">Affiliation</div>
            <div class="group-items">
              <a
                href="/api/bcl-affiliation/analytics/preview"
                class="{{#if (eq active 'bcl-affiliation')}}active{{/if}}"
              >
                Home
              </a>

              <a
                href="/api/bcl-affiliation/tree/preview"
                class="{{#if (eq active 'bcl-affiliation-tree')}}active{{/if}}"
              >
                Tree
              </a>
            </div>
          </div>

          <div class="group">
            <div class="group-title">BCL</div>
            <div class="group-items">
              <a
                href="/api/analytics/preview"
                class="{{#if (eq active 'bcl')}}active{{/if}}"
              >
                Core
              </a>
            </div>
          </div>
        </div>
      </aside>

      <main class="content">
        <div class="topbar">
          <h1>{{title}}</h1>
          {{#if topRight}}
            <span class="pill mono">{{topRight}}</span>
          {{/if}}
        </div>

        {{> @partial-block}}
      </main>
    </div>
  </body>
</html>


