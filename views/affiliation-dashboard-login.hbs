<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; font-family: "Inter", sans-serif; }
      body {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #0f172a;
        color: #e2e8f0;
        padding: 1rem;
      }
      .card {
        width: 100%;
        max-width: 360px;
        background: #1e293b;
        border-radius: 0.9rem;
        padding: 1.75rem;
        box-shadow: 0 18px 40px rgba(0, 0, 0, 0.35);
      }
      .brand { display: flex; align-items: center; gap: 0.6rem; margin-bottom: 1.25rem; }
      .logo {
        width: 34px; height: 34px; border-radius: 10px;
        background: linear-gradient(135deg, rgba(59,130,246,0.9), rgba(139,92,246,0.9));
      }
      h1 { font-size: 1rem; font-weight: 600; color: #f1f5f9; }
      .sub { font-size: 0.78rem; color: #94a3b8; margin-top: 2px; }
      label {
        display: block; font-size: 0.78rem; color: #cbd5e1;
        margin: 0.85rem 0 0.3rem;
      }
      input {
        width: 100%; padding: 0.6rem 0.7rem; border-radius: 0.5rem;
        border: 1px solid rgba(148, 163, 184, 0.28);
        background: #0f172a; color: #f1f5f9; font-size: 0.9rem;
      }
      input:focus { outline: 2px solid rgba(59, 130, 246, 0.55); outline-offset: 1px; }
      button {
        width: 100%; margin-top: 1.15rem; padding: 0.65rem;
        border: 0; border-radius: 0.5rem; cursor: pointer;
        background: #3b82f6; color: #fff; font-size: 0.9rem; font-weight: 600;
      }
      button:hover { background: #2563eb; }
      .error {
        margin-top: 1rem; padding: 0.6rem 0.7rem; border-radius: 0.5rem;
        background: rgba(239, 68, 68, 0.12);
        border: 1px solid rgba(239, 68, 68, 0.35);
        color: #fecaca; font-size: 0.8rem;
      }
      .note { margin-top: 1.1rem; font-size: 0.72rem; color: #64748b; line-height: 1.45; }
    </style>
  </head>
  <body>
    <form class="card" method="post" action="/api/bcl-affiliation/auth/login">
      <div class="brand">
        <div class="logo"></div>
        <div>
          <h1>Affiliation dashboards</h1>
          <div class="sub">Internal — sign in to continue</div>
        </div>
      </div>

      {{#if error}}
        <div class="error">{{error}}</div>
      {{/if}}

      <label for="username">Username</label>
      <input
        id="username"
        name="username"
        autocomplete="username"
        autocapitalize="none"
        autocorrect="off"
        spellcheck="false"
        value="{{username}}"
        required
        autofocus
      />

      <label for="password">Password</label>
      <input
        id="password"
        name="password"
        type="password"
        autocomplete="current-password"
        required
      />

      <button type="submit">Sign in</button>

      <p class="note">
        These pages show wallet addresses alongside X handles and payouts. Do not
        share this login.
      </p>
    </form>
  </body>
</html>
